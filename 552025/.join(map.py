.join(map(str, my_list))  # Convert each element to a string and join them with a space
map(str, my_list): This converts each element of the list to a string.

' '.join(...): This joins the string elements of the list with a space ' ' between them